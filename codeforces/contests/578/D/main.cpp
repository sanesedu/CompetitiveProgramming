
#include <iostream>

using namespace std;

int main() {

    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    int k, n;
    cin >> n >> k;

    string arr[n] = {};

    long long vals[n][n] = {};
    long long max =

    for(int i = 0; i < n; ++i) {
        cin >> arr[i];
    }



    return 0;
}
